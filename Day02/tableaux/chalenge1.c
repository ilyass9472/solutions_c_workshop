#include <stdio.h>
#include <math.h>

int main()
{
    // int ilyss[6] = {1, 2, 3, 4, 5, 6};
    int ilyass = 2;


    for (int i = 0; i <= 10; i++)
    {
        // printf("number : %d\n", ilyss[i]);
        printf("%d\n", pow(ilyass, i));

    }

    return 0;
}