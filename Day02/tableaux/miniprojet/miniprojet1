#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#define MAX_LIVRES 100
#define MAX_CHAR 100

struct Livre {
    char titre[MAX_CHAR];
    char auteur[MAX_CHAR];
    float prix;
    int quantite;
};

struct Livre livres[MAX_LIVRES];
int livresCount = 0;

int ajouterLivre() {
    // ... (similar to the original code, with input validation)
}

int afficherLivre() {
    // ... (similar to the original code)
}

int rechercherLivre(const char *titre) {
    // ... (similar to the original code)
}

int mettreAJourLivre(const char *titre, int nouvelleQuantite) {
    // ... (similar to the original code)
}

int supprimerLivre(const char *titre) {
    // ... (similar to the original code)
}

int checkStock() {
    // ... (similar to the original code)
}

int terminal() {
    // ... (similar to the original code)
}

int main() {
    terminal();
    return 0;
}